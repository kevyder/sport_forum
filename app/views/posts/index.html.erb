<% @posts.each do |post|%>
  <div class="col-md-10 col-md-offset-1">
    <div class="message">
      <h2 class="message-title"><%= post.title %></h2>
      <%= link_to "Join Conversation", post, class: "btn-custom" %>
    </div>
  </div>
<% end %>


<%= link_to "New Post", new_post_path %>